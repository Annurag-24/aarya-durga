<IfModule mod_rewrite.c>
    RewriteEngine On

    # If the request is already targeting /public, do not rewrite again.
    RewriteCond %{REQUEST_URI} !^/public/

    # Route all requests through the Laravel public directory.
    RewriteRule ^(.*)$ public/$1 [L]

</IfModule>
